import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const SliderLists = () => {
  return (
    <View style={styles.container}>
      <Text>SliderLists</Text>
    </View>
  );
};

export default SliderLists;

const styles = StyleSheet.create({
  container: {
    height: 310,
    width: '100%',
    backgroundColor: 'red',
  },
});
